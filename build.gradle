buildscript {
    repositories {
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.0.1'
    }
}

plugins {
    id 'com.quittle.setup-android-sdk' version '1.6.0'
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}

defaultTasks 'assemble', 'check', 'bundle'
