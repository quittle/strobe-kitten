buildscript {
    repositories {
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.6.2'
    }
}

plugins {
    id 'com.quittle.setup-android-sdk' version '1.5.0'
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}

defaultTasks 'assemble', 'check', 'bundle'
